import { CerrarSesionPipe } from './cerrar-sesion.pipe';

describe('CerrarSesionPipe', () => {
  it('create an instance', () => {
    const pipe = new CerrarSesionPipe();
    expect(pipe).toBeTruthy();
  });
});
